const { User, Book} = require('../models');

const resolvers = {
  Query: {
    // users: async () => {
    //   return await User.find({});
    // },
    // books: async (parent, { _id }) => {
    //   const params
    // }
  }
};

module.exports = resolvers;
